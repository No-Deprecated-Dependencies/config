import type { Config } from 'prettier'

export default {
    endOfLine: 'auto',
    printWidth: 120,
    proseWrap: 'always',
    semi: false,
    singleAttributePerLine: true,
    singleQuote: true,
    tabWidth: 4,
    trailingComma: 'all',
} satisfies Config
